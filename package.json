{
  "name": "tessera",
  "version": "0.1.0",
  "type": "module",
  "bin": { "tessera": "bin/tessera.mjs" },
  "description": "Portable per-scope coordination for concurrent local Claude Code agents: append-only awareness bus + git worktrees + flock for shared non-mergeable files + Claude hooks. Zero runtime dependencies.",
  "license": "MIT",
  "engines": { "node": ">=18" }
}
